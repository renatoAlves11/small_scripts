#include <stdio.h>
#define N 30

float processoDepartamento(int codDepartamento, int qtdFuncionarios)
{
   int i, matricula;
   float salario, somaSalario = 0;
   for(i=0; i<qtdFuncionarios; i++) {
     
     scanf()
   }
  
}

int main ()
 { 
   int i, codDepartamento, qtdFuncionarios;
   float somaSalarioTotal = 0;
   for(i=0;i<N;i++) {
     scanf("%d%d", &codDepartamento, &qtdFuncionarios);
     somaSalarioTotal += processoDepartamento(codDepartamento, qtdFuncionarios); 
   
   }
   printf("%f\n", somaSalario)
  
return 0;  
}